## Instructions

Generate some certificates to be used by the HTTPS and TLS servers:

```bash
./generate_certs.sh
```

Start the server:

```bash
npm start
```

Set up channels in the OpenHIM:

```bash
# TODO: Bash script
```
